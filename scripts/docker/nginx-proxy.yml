---
version: '2'
services:
 nginx:
   image: jwilder/nginx-proxy
   container_name: nginx
   volumes:
   ports:
     - "80:80"
     - "443:443"
   restart: always
   environment:
     - PUID=1000
     - PGID=1000
networks:
  default:
    external:
      name: plexguide