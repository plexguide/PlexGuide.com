---
version: '2'
services:
 support:
   image: admin9705/support
   container_name: support
   ports:
     - "45560:45560"
   volumes:
     - /opt/appdata/support:/support
   restart:
     on-failure
   environment:
     - PUID=1000
     - PGID=1000
