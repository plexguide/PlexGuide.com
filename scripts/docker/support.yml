---
version: '2'
services:
 support:
   image: admin9705/support
   container_name: support
   ports:
     - "12000:12000"
   volumes:
     - /opt/appdata/support:/support
   restart:
     always
   environment:
     - PUID=1000
     - PGID=1000
