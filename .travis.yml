language: bash
sudo: enabled

before_install:
- sudo apt-get install wget -y && sudo wget https://plexguide.com/install.sh && sudo bash ./install.sh
- sudo bash /opt/plexguide/roles/baseline/scripts/ansible.sh

script:
- ansible-playbook /opt/plexguide/pg.yml --tags portainer
