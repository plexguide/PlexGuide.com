---
- hosts: localhost
  vars:
    extra: ''

  roles:
    - { role: docker, tags: ['docker'] }
    - { role: rcloneinstall, tags: ['rcloneinstall'] }
    - { role: gcloud_sdk, tags: ['gcloud_sdk'] }
