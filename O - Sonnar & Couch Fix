################# Post YML Fix ################# START
### Problem:  Couch and Sonnar are unable to see the Google Drive via RCLONE
### Reason:  RClone loads up mores slowly, so the containers are too fast
### Answer:  Force a reboot of those two containers after 20 seconds
### Result:  They reboot and both containers should see the RCLONE Script

#!/bin/bash
x=20
while [ $x -gt 0 ]
do
    sleep 1s
    clear
    echo "$x seconds until blast off"
    x=$(( $x - 1 ))
done
docker restart couchpotato
docker restart sonarr

sudo apt-get install curl
bash <(curl -Ss https://my-netdata.io/kickstart-static64.sh)

################# Post YML FIX ################# END
