---
- hosts: localhost
  gather_facts: false
  tasks:

    - name: Cloning PlexGuide
      git:
        repo: "https://github.com/Admin9705/PlexGuide-Community"
        dest: /opt/communityapps
        version: "master"
        force: yes
