---
image: ubuntu

environment:
  PG: "/opt/plexguide"

install:
  - sh: mkdir /opt/plexguide
  - sh: cd ${PG} && mv ./opt/plexguide/;
  - sh: sudo bash ${PG}/roles/baseline/scripts/ansible.sh

matrix:
  fast_finish: true

build: on
