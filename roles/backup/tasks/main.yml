#!/bin/bash
#
# [Ansible Role]
#
# GitHub:   https://github.com/Admin9705/PlexGuide.com-The-Awesome-Plex-Server
# Author:   Admin9705 & Deiteq
# URL:      https://plexguide.com
#
# PlexGuide Copyright (C) 2018 PlexGuide.com
# Licensed under GNU General Public License v3.0 GPL-3 (in short)
#
#   You may copy, distribute and modify the software as long as you track
#   changes/dates in source files. Any modifications to our software
#   including (via compiler) GPL-licensed code must also be made available
#   under the GPL along with build & install instructions.
#
#################################################################################
---
- include_vars: variables.yml

- name: "Building List"
  shell: "cat /tmp/backup.build"
  register: backlist
  tags:
    - backup_normal

- name: "Pulling Selected Program"
  shell: "cat /tmp/program_var"
  register: backlist
  tags:
    - backup_normal
  #ignore tag

- name: "Server ID for Backup"
  shell: "cat /var/plexguide/server.id"
  register: server_id
  tags:
    - backup_normal

############################################ SWITCH ON - Manual Selected
- pause:
    prompt: "\n[Detected Programs]: {{backlist.stdout}}\n\nTYPE the name of a PROGRAM to Backup! (Case Senstive)"
  register: program_var.user_input
  when: switch == "off"

- name: "Establishing Variable"
  shell: "echo {{switch}}"
  when: switch == "off"

############################################ SWITCH OFF - Automatic Process

- name: "Establishing Variable"
  shell: "echo {{program_var.stdout}} > /tmp/program_var"
  when: switch == "on"

- name: "Establishing Final Variable"
  shell: "awk '{print $1}' /tmp/program_var"
  register: program_var
  when: switch == "on"

############################################ REGISTER FINAL Variable

### TEST
#- set_fact:
#    tomcat_value: "{{ "corn" if (filepath == '/var/opt/tomcat_1') else "bacon" }}"


- debug: msg="Temp Message - For {{program_final}} - Server ID {{server_id.stdout}}"
  tags:
    - backup_normal

- name: "Checking {{program_final.stdout}} location in APPDATA"
  stat:
    path: /opt/appdata/{{program_final.stdout}}
  register: programappdata
  tags:
    - backup_normal

- name: "Checking if RClone Encrypted"
  stat:
    path: /var/plexguide/rclone-en.yes
  register: encrypted
  tags:
    - backup_normal

- name: Stopping Docker Container - {{program_final.stdout}}
  docker_container:
    name: "{{program_final.stdout}}"
    state: stopped
  ignore_errors: True
  when: programappdata.stat.exists == True
  tags:
    - backup_normal

- debug: msg="Time Length can be Long or Short! Please Wait!"
  when: programappdata.stat.exists == True
  tags:
    - backup_normal

########### FOR PLEX - START
- name: Backing Up Program to GDrive - plex
  shell: "tar --warning=no-file-removed --warning=no-file-changed --ignore-failed-read --exclude='./database/Library/Application Support/Plex Media Server/Cache/PhotoTranscoder' -zcvf /tmp/plex.tar -C /opt/appdata/plex ."
  ignore_errors: True
  tags:
    - backup_plex
########### FOR PLEX - END // BACKUP OTHER - START
- name: Backing Up Program to GDrive - {{program_final.stdout}}
  archive:
    path:
    - /opt/appdata/{{program_final.stdout}}/*
    dest: /tmp/{{program_final.stdout}}.tar
    format: tar
  tags:
    - backup_other
############ BACKUP OTHER - END
- name: Restart Docker Container - {{program_final.stdout}}
  docker_container:
    name: "{{program_final.stdout}}"
    state: started
  ignore_errors: True
  when: programappdata.stat.exists == True
  tags:
    - backup_normal

- name: Uploading Backup to Google Drive
  shell: "rclone copy /tmp/{{program_final.stdout}}.tar gdrive:/plexguide/backup/{{server_id.stdout}} -v --checksum --drive-chunk-size=64M"
  when: encrypted.stat.exists == False and programappdata.stat.exists == True
  tags:
    - backup_normal

- name: Uploading Backup to Google Drive (encrypted)
  shell: "rclone copy /tmp/{{program_final.stdout}}.tar gcrypt:/plexguide/backup/{{server_id.stdout}} -v --checksum --drive-chunk-size=64M"
  when: encrypted.stat.exists == True and programappdata.stat.exists == True
  tags:
    - backup_normal

- name: Remove TEMP Backup!
  file:
    path: "{{ item }}"
    state: absent
  with_items:
    - /tmp/{{program_final.stdout}}.tar
  when: programappdata.stat.exists == True
  tags:
    - backup_normal

#- name: Kill Switch
#  shell: echo "false" > /tmp/alive

- debug: msg="Program is now backed up to your Google Drive - {{program_final.stdout}}"
  when: programappdata.stat.exists == True
  tags:
    - backup_normal

- debug: msg="The Program's Data DOES NOT EXIST Locally - Cannot Backup {{program_final.stdout}}"
  when: programappdata.stat.exists == False
  tags:
    - backup_normal
