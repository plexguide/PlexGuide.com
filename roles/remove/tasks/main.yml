---
- hosts: localhost
  gather_facts: false
  tasks:

  - name: Set PGRole
    shell: "cat /tmp/program_var"
    register: pgrole

  - cron:
      name: "{{pgrole}}"
      state: absent
    ignore_errors: yes
