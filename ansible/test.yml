---
- hosts: localhost

## ensure to delete traefik from portainer
## ansible-playbook /opt/plexguide/ansible/test.yml --tags nginx
## ansible-playbook /opt/plexguide/ansible/test.yml --tags sonarrssl

  roles:
  - { role: traefik, tags: ['traefik'] }
  - { role: lazylibrarian, tags: ['lazylibrarian'] }
  - { role: ssl, tags: ['ssl'] }
  - { role: rutest, tags: ['rutest'] }
  - { role: vesta, tags: ['vesta'] }
  - { role: nginx, tags: ['nginx'] }
  - { role: docker, tags: ['docker'] }
  - { role: sonarrssl, tags: ['sonarrssl'] }
