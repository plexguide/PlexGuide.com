---
- hosts: localhost
  vars_files:
    - var.yml
## ensure to delete traefik from portainer
## ensure to update plexguide
## sudo ansible-playbook /opt/plexguide/ansible/test.yml --tags docker
## sudo ansible-playbook /opt/plexguide/ansible/test.yml --tags nginx
## sudo ansible-playbook /opt/plexguide/ansible/test.yml --tags sonarrssl

  roles:
  - { role: traefik, tags: ['traefik'] }
  - { role: lazylibrarian, tags: ['lazylibrarian'] }
  - { role: ssl, tags: ['ssl'] }
  - { role: rutest, tags: ['rutest'] }
  - { role: vesta, tags: ['vesta'] }
  - { role: nginx, tags: ['nginx'] }
  - { role: docker, tags: ['docker'] }
  - { role: sonarrssl, tags: ['sonarrssl'] }
