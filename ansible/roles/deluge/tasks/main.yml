#!/bin/bash
#
# [Ansible Role]
#
# GitHub:   https://github.com/Admin9705/PlexGuide.com-The-Awesome-Plex-Server
# Author:   Admin9705 & Deiteq & Flicker-Rate
# URL:      https://plexguide.com
#
# PlexGuide Copyright (C) 2018 PlexGuide.com
# Licensed under GNU General Public License v3.0 GPL-3 (in short)
#
#   You may copy, distribute and modify the software as long as you track
#   changes/dates in source files. Any modifications to our software
#   including (via compiler) GPL-licensed code must also be made available
#   under the GPL along with build & install instructions.
#
#################################################################################
---
- name: Remove Deluge Container
  docker_container:
    name: deluge
    state: absent

- name: Create Deluge Directory
  file: "path=/opt/appdata/deluge/config state=directory mode=0775 owner=1000 group=1000 recurse=true"

################ REGISTER
- name: Check extractor.conf exists
  stat:
    path: "/opt/appdata/deluge/config/extractor.conf"
  register: extractor.conf
  tags:
    - extractor

- name: Check label.conf exists
  stat:
    path: "/opt/appdata/deluge/config/label.conf"
  register: label.conf
  tags:
    - noseed

- name: Check web.conf exists
  stat:
    path: "/opt/appdata/deluge/config/label.conf"
  register: label.conf
  tags:
    - web
    
################ MOVE
- name: Move noseed.conf
  template:
    src: noseed.conf
    dest: /opt/appdata/deluge/config/label.conf
    force: yes
  tags:
    - noseed

- name: Move extractor.conf
  template:
    src: extractor.conf
    dest: /opt/appdata/deluge/config/extractor.conf
    force: yes
  tags:
    - extractor

- name: Move web.conf
  template:
    src: web.conf
    dest: /opt/appdata/deluge/config/web.conf
    force: yes
  tags:
    - web

- name: Create Deluge Container
  docker_container:
    name: deluge
    image: binhex/arch-deluge
    pull: yes
    cpu_shares: 256
    published_ports:
      - "{{ports}}8112:8112"
      - "58846:58846"
      - "58946:58946"
    env:
      PUID: "1000"
      PGID: "1000"
    networks:
      - name: plexguide
        aliases:
          - deluge
    labels:
      traefik.enable: "true"
      traefik.frontend.rule: "Host:deluge.{{domain}}"
      traefik.port: "8112"
    volumes:
      - /opt/appdata/deluge/config:/config
      - /mnt/deluge/temp:/temp
      - /mnt/deluge/downloaded:/downloaded
      - /mnt/deluge/torrent:/torrent
      - /etc/localtime:/etc/localtime:ro
      - /mnt/unionfs/:/unionfs
    restart_policy: always
    state: started