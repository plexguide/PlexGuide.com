---
  - name: Removing File Directories
    file:
      state: absent
      path: "{{ item }}"
    with_items:
        - /var/plexguide
        - /opt/appdata

  - name: Remove via Shell (Be Patient)
    shell: "rm -r /mnt/*"