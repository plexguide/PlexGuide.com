---
  - name: Removing File Directories
    file:
      state: absent
      path: "{{ item }}"
    with_items:
        - /var/plexguide
        - /opt/appdata

  - name: Remove via Shell
    shell: "rm -r /mnt/*"