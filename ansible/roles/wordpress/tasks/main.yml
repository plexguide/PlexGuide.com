#!/bin/bash
#
# [Ansible Role]
#
# GitHub:   https://github.com/Admin9705/PlexGuide.com-The-Awesome-Plex-Server
# Author:   Admin9705
# URL:      https://plexguide.com
#
# PlexGuide Copyright (C) 2018 PlexGuide.com
# Licensed under GNU General Public License v3.0 GPL-3 (in short)
#
#   You may copy, distribute and modify the software as long as you track
#   changes/dates in source files. Any modifications to our software
#   including (via compiler) GPL-licensed code must also be made available
#   under the GPL along with build & install instructions.
#
#################################################################################
---
- include_role:
    name: variables

- name: Register WP Port
  shell: "cat /var/plexguide/wpport.id"
  register: wpport

- name: Register WP ID
  shell: "cat /var/plexguide/wp.id"
  register: wpid

- name: Register WP Subdomain
  shell: "cat /var/plexguide/wpsubdomain.id"
  register: wpsub

- name: Remove Wordpress Container
  docker_container:
    name: wordpress
    state: absent

- name: Remove Database Container
  docker_container:
    name: my-wpdb
    state: absent

- name: Deploy Wordpress Database
  docker_container:
    name: my-wpdb-{{wpid.stdout}}
    image: mariadb
    pull: yes
    cpu_shares: 512
    published_ports:
      - "127.0.0.1:10{{wpport.stdout}}:3306"
    volumes:
      - "/etc/localtime:/etc/localtime:ro"
      - "db_data:/var/lib/mysql"
    env:
      MYSQL_ROOT_PASSWORD: "test123"
    networks:
      - name: plexguide
        aliases:
          - wpdb-{{wpid.stdout}}
    restart_policy: always
    state: started

- name: Deploy Wordpress Container
  docker_container:
    name: wordpress-{{wpid.stdout}}
    image: wordpress
    pull: yes
    cpu_shares: 256
    published_ports:
      - "9{{wpport.stdout}}:80"
    env:
      APP: wordpress
      PUID: "1000"
      PGID: "1000"
      WORDPRESS_DB_PASSWORD: "test123"
    networks:
      - name: plexguide
        aliases:
          - wordpress-{{wpport.stdout}}
    links:
      - my-wpdb-{{wpid.stdout}}:mysql
    volumes:
      - /opt/appdata/wordpress/{{wpid.stdout}}:/var/www/html
      - /etc/localtime:/etc/localtime:ro
    restart_policy: always
    state: started
    labels:
      traefik.enable: "true"
      traefik.frontend.redirect.entryPoint: "https"
      traefik.frontend.rule: "Host:{{wpid.stdout}}.{{domain.stdout}}"
      traefik.port: "80"

##### STORE INFO
- name: Store WP Port
  shell: "echo '9{{wpport.stdout}}' > /opt/appdata/wordpress/{{wpid.stdout}}/pgwpport"

- name: Store DB Port
  shell: "echo '10{{wpport.stdout}}' > /opt/appdata/wordpress/{{wpid.stdout}}/pgdbport"

- name: Store WP Subdomain
  shell: "echo '{{wpsub.stdout}}' > /opt/appdata/wordpress/{{wpid.stdout}}/pgsub"

##### OutPut Info
- debug: msg="Database Cannot Be Accessed; Deployed Internal via Port 10{{wpport.stdout}}"

- debug: msg="Access WP Via Port 9{{wpport.stdout}} - SubDomain {{wpsub.stdout}}"