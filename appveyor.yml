---
image: ubuntu

environment:
  PG: "/opt/plexguide"

install:
  - sh: mkdir /opt/plexguide
  - sh: mv /home/appveyor/projects/plexguide-com-the-awesome-plex-server/* /opt/plexguide/
  - sh: cd ${PG}
  - sh: sudo bash ${PG}/roles/baseline/scripts/ansible.sh

matrix:
  fast_finish: true

build: on
